# Sons / Audio

Tous les sons sont générés dynamiquement via l'API Web Audio (oscillateurs + bruit).
Aucun fichier audio n'est requis.

Pour remplacer par vos propres sons MP3/OGG, modifiez les fonctions
`playGoalSound()`, `playCoinSound()`, `playKickSound()` dans `/public/client.js`.
